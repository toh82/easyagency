define(['backbone', 'database'], function(Backbone, fuckyou) {
console.log(fuckyou);
    var App = Backbone.View.extend({
        initialize: function() {
            
        }
    });

    return App;
});