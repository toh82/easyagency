define([
    'jquery',
    'backbone',
    'underscore'
], function($, Backbone, _) {
    

    var notesView = Backbone.View.extend({
        id: "notes-list",
        initialize: function() {        
			
        }
    });

    return notesView;
});